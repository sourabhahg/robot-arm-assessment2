# robot-arm-assessment2
This project verifies that a robot arm system is doing the right thing. It reads the input file the system was given and the output file it produced, then checks whether the rectangle work area is valid, whether the arm visited the right coordinates, stayed inside its boundaries,.
